{
  "name": "make-it-blink-build",
  "scripts": {
    "dev": "vite",
    "build": "npm-run-all -l clean --parallel build:* ",
    "build:vite": "vue-tsc --noEmit && vite build",
    "clean": "rimraf ../dist"
  },
  "dependencies": {
    "vue": "^3.2.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.9.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3",
    "vite": "^2.5.10",
    "vue-tsc": "^0.3.0"
  }
}
